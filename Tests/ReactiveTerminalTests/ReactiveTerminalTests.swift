import XCTest
@testable import ReactiveTerminal

final class ReactiveTerminalTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ReactiveTerminal().text, "Hello, World!")
    }
}
